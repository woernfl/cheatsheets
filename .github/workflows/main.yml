name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-18.04
    
    steps:
    - uses: actions/checkout@v1
    - name: Run a multi-line script
      run: |
        mkdir -p cheat
        cp docs/* cheat
        find docs/ -type f -exec sed 's/a//'
